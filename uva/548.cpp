#include <iostream>
using namespace std;

struct tree
{
  int n;
  tree* left;
  tree* right;

  tree(int n)
  {
    this->n = n;
    left = right = NULL;
  }
};

int in[10000];
int pre[10000];
int idx[10000];

void reconstruction(tree* t, int in[], int pre[], int l, int r)
{
  
}

int main()
{
  for (int i = 0; i < size; i++)
  {
    
  }
  tree* t = new tree(); 

  return 0;
}
